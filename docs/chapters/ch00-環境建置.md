# Ch0 — 環境建置

**成果：** `GET /health` 回傳 `{"status":"ok","database":"connected"}`，並有一支 E2E 測試守住它。

> 專案層級的參考（指令速查、檔案地圖、換機接續、程式碼閱讀動線）在 [`docs/專案速查.md`](../專案速查.md)。
> 這個專案最初是怎麼從空資料夾建起來的，見 [`docs/從零建置.md`](../從零建置.md)。

---

## 核心概念

### 依賴注入（Dependency Injection, DI）

這是理解 NestJS 的第一把鑰匙。看這行：

```ts
constructor(private readonly prisma: PrismaService) {}
```

你**沒有**寫 `new PrismaService()`。你只是宣告「我需要一個 PrismaService」，由 NestJS 負責建立實例並傳進來。

為什麼要這樣？三個實際好處：

1. **單例共享** — 整個應用只會有一個 `PrismaService` 實例，也就是只有一個資料庫連線池。如果每個 controller 各自 `new` 一個，連線數會爆掉。
2. **可替換** — 測試時可以叫 Nest 換成假的實作，不用改被測程式碼。
3. **依賴關係外顯** — 看建構子就知道這個類別依賴什麼，不用翻遍整個檔案找 `new`。

**Nest 怎麼知道要注入什麼？** 靠 TypeScript 編譯時寫入的型別 metadata。這就是 `tsconfig.json` 裡 `emitDecoratorMetadata: true` 的用途 —— 它讓 tsc 把「這個參數的型別是 `PrismaService`」這件事編譯進 JS，Nest 在執行期讀出來。

> 這也是為什麼**型別註記在 NestJS 裡不是可選的**。拿掉 `: PrismaService`，Nest 就不知道要注入什麼，會直接報錯。前端寫 TS 時型別純粹給編譯器看，後端在這裡型別會影響執行期行為 —— 這是心態上要調整的一點。

### Module — 應用的組裝單位

```ts
@Module({
  imports: [ConfigModule, PrismaModule, HealthModule],  // 我依賴誰
  controllers: [HealthController],                       // 我提供哪些路由
  providers: [PrismaService],                            // 我內部有哪些可注入的服務
  exports: [PrismaService],                              // 我允許別人注入我的哪些服務
})
```

關鍵規則：**一個 module 只能注入「自己 providers 裡有的」或「imports 進來的 module 有 exports 的」東西。** 沒 export 就等於 private。

這跟前端的 `export` / `import` 概念相通，只是粒度是「模組」而不是「檔案」。

### 生命週期鉤子

```ts
async onModuleInit()    { await this.$connect() }     // module 建好時
async onModuleDestroy() { await this.$disconnect() }  // 應用關閉時
```

搭配 `main.ts` 的 `app.enableShutdownHooks()`，Ctrl+C 時才會真的觸發 `onModuleDestroy`。

**為什麼重要：** 沒有正確斷線的話，Jest 測試跑完不會結束（連線池還開著，Node 的 event loop 不空），部署到雲端每次重啟也會累積殭屍連線。Neon 免費方案有連線數上限，這會直接變成故障。

### 兩條時間軸 —— 把上面三個概念串起來

DI、module、生命週期各自看得懂之後，還缺一件事：**它們分別發生在什麼時候。**

閱讀動線上的九個檔案不是「請求依序經過的九站」，而是分屬兩個完全不同的時刻。分不清這件事，前面三個概念就會一直是浮的。

**時刻 A —— 啟動（`pnpm start:dev` 的那幾秒，只發生一次）**

1. Node 執行 `main.ts`，`void bootstrap()` 啟動流程
2. `NestFactory.create(AppModule)` 讀取 `@Module` 貼的 metadata，往下展開 `imports`
3. `ConfigModule.forRoot()` 把 `.env` 讀進 `process.env`，提供全域的 `ConfigService`
4. 建立 `PrismaService` —— 它的建構子要 `ConfigService`，所以 Nest **先確保 `ConfigService` 存在**。
   建構子裡 `new PrismaPg({ connectionString })` 只是把 `pg.Pool` 物件建出來，**沒有任何實體連線**
5. 建立 `HealthController` —— 靠 `emitDecoratorMetadata` 寫入的參數型別找到 `PrismaService` 並傳進去。
   **依賴注入就發生在這一刻。** 同時掃描 `@Controller('health')` 與 `@Get()`，
   在路由表登記 `GET /health → HealthController.check`
6. 所有零件就緒 → Nest 呼叫 `onModuleInit` → `$connect()`（仍然沒有真的驗證帳密）
7. `app.enableShutdownHooks()` 掛上 SIGINT / SIGTERM 監聽
8. `app.listen(3100)` 綁定 port，process 從此不結束

> **第 4、5 步的順序是 Nest 從依賴關係推導出來的，不是 `app.module.ts` 的 `imports` 陣列順序。**
> 你只宣告「誰需要誰」，順序是結果而不是輸入。

**時刻 B —— 每個請求（每秒可以發生一千次）**

1. `GET /health` → Node http server → Express → Nest 比對路由表 → `HealthController.check()`
2. **這裡沒有建立任何東西。** `this.prisma` 是時刻 A 就塞好的，`check()` 只是一個普通的方法呼叫
3. `$queryRaw` → `PrismaPg` adapter → `pg.Pool` **此刻才真的 acquire 一條實體連線**
4. 回傳的普通物件被 Nest 序列化成 JSON、補上 200
5. 連線**還回 pool 而不是關閉**，下一個請求重複使用

**時刻 C —— 關閉（`Ctrl+C`）**

SIGINT → `enableShutdownHooks` 攔截 → `onModuleDestroy` → `$disconnect()` 釋放連線池 → process 結束。

---

最常見的誤解是以為「請求進來時 Nest 才去找 `PrismaService`」。不是 —— 請求進來時一切都已就位。

**分不清一件事屬於哪個時刻時，問自己：這件事一秒鐘會發生一千次嗎？**

另外要記得，閱讀動線上有三個檔案**兩條軸都不在**：

| 檔案 | 何時被讀 |
| --- | --- |
| `prisma/schema.prisma` | 只有手動跑 `prisma generate` / `migrate` 時，服務執行期完全用不到 |
| `prisma.config.ts` | 同上，而且只給 CLI 用 |
| `test/health.e2e-spec.ts` | 只有 `pnpm test:e2e` 時 |

它們排在動線裡，是為了解釋「`PrismaClient` 那些方法是怎麼來的」，不是因為請求會經過它們。

### `app.init()` 與 `app.listen()` 的差別

E2E 測試裡沒有 `app.listen()`，只有 `app.init()`，但 supertest 照樣發得出真實請求。這是怎麼回事：

| | 做什麼 |
| --- | --- |
| `app.init()` | 建立所有零件、跑完 DI、把路由表註冊到底層 Express。**做完就停，不碰網路。** |
| `app.listen(port)` | 先做完上面全部，**再**叫作業系統綁定 port 開始接連線 |

所以 `init()` 之後，記憶體裡是一個功能完整、但「沒有門牌號碼」的應用。

`app.getHttpServer()` 拿到的就是那個還沒監聽的 `http.Server`。supertest 收到它之後替它呼叫 `server.listen(0)` —— `0` 是作業系統的特殊值，意思是「隨便給我一個沒人用的 port」。它對那個臨時 port 發出**真實的 HTTP 請求**，收到回應後把 server 關掉。

**這樣設計的兩個好處：**

1. 測試不會跟開發伺服器搶 3100，`pnpm start:dev` 掛著也能跑 `pnpm test:e2e`
2. 之後多個 e2e 檔（`surveys` / `responses`……）被 Jest 平行執行時，各自拿到不同的臨時 port，互不干擾

**為什麼要強調它「真的走了 HTTP」：** 這正是 E2E 測試的價值所在。走完整一趟才驗證得到路由組合出來的網址對不對、物件有沒有被正確序列化成 JSON、狀態碼是不是 200，以及 Ch2 之後的 DTO 驗證、Exception Filter、Guard 這些**中介層**有沒有真的生效。

如果只是直接呼叫 `check()` 方法比對回傳值，上面每一項都測不到 —— 那就退化成單元測試了。這是「E2E 優先」在機制層面的具體理由。

### Driver Adapter（Prisma 7 的重大改變）

Prisma 6 以前：Prisma 自己用 Rust 引擎連資料庫。
Prisma 7 起：**必須**透過 driver adapter，用 Node 生態的資料庫驅動（這裡是 `pg`）。

```ts
const adapter = new PrismaPg({ connectionString });
super({ adapter });
```

網路上找得到的 NestJS + Prisma 教學幾乎都是 v5/v6 的 `super({ datasources: ... })` 寫法，在 v7 會直接壞掉。**查資料時務必確認版本。**

**連帶的行為改變：連線變成 lazy。** adapter 底下是 `pg.Pool`，而 Pool 只在第一次查詢時才真的去 acquire 連線。所以 `onModuleInit` 裡的 `$connect()` 在 adapter 模式下並不會拿帳密去跟資料庫驗證 —— 密碼錯的服務**可以完全正常啟動**，要等到第一個真實查詢才爆炸（見下方作業第 2 題）。

---

## 決策取捨

### 為什麼 `PrismaModule` 用 `@Global()`

一般來說 `@Global()` 是要避免的 —— 它讓依賴關係變隱晦，看某個 module 的 `imports` 不再能得知它真正依賴什麼。

但資料庫連線是**整個應用共用的單一資源**，而且幾乎每個 feature module 都需要。若不用 `@Global()`，往後每個新 module 都得手動 import 一次 `PrismaModule`，純粹是噪音。

**判斷標準：** 只有「全應用單例、幾乎人人都要用、且不會有第二種實作」的東西才適合 `@Global()`。資料庫連線與設定服務符合，業務邏輯服務不符合。

### 為什麼 TypeScript 從 7 降到 5.9

專案初始裝的是 TypeScript 7（Go 重寫的編譯器）。但 NestJS 的 DI 依賴 `emitDecoratorMetadata`，這條路徑在 TS 7 尚未經過驗證。

**取捨：** 學習專案的目標是學會 NestJS 與資料庫，不是當工具鏈的白老鼠。降版是用「放棄一點新特性」換「排除一整類難以診斷的問題」。

**通則：** 當你同時在學習新東西 A 和使用未驗證的新工具 B，出問題時你無法判斷是 A 沒學好還是 B 有 bug。**一次只引入一個未知數。**

### 為什麼 Prisma client 要輸出到 `src/generated/`

TypeScript 的 `rootDir` 是「所有輸入檔案的共同祖先目錄」自動推導出來的。若 client 產生在根目錄的 `generated/`，`rootDir` 就會從 `src/` 被撐大到專案根目錄，編譯輸出變成 `dist/src/main.js`，`node dist/main` 就找不到進入點。

放進 `src/` 底下讓所有輸入檔案共用同一個根，路徑就穩定了。

**同樣的道理**，`prisma.config.ts` 在專案根目錄，也必須在 `tsconfig.build.json` 排除，否則同一個問題會再發生一次。**以後在根目錄新增任何 `.ts` 檔都要留意這點。**

### 為什麼 `moduleFormat = "cjs"`

`prisma-client` generator 預設輸出 ESM，程式碼裡會用到 `import.meta.url`。而 NestJS 預設編譯成 CommonJS。Node 一偵測到 `import.meta` 就把該檔案當 ES module 載入，於是 CommonJS 的 `exports` 變成未定義 → 執行期爆炸。

**取捨：** 兩種方向都可行 —— 把整個專案改成 ESM，或把 Prisma 輸出改成 CJS。選後者，因為 NestJS 生態（尤其 Jest）在 CommonJS 下摩擦最小，而學習專案不該把預算花在模組系統的邊界問題上。

### 為什麼 E2E 優先於單元測試

後端的價值在於「給定一個 HTTP 請求，回傳正確的結果並正確地改變資料庫狀態」。E2E 測試驗證的正是這件事，而且不 mock 任何東西 —— 連 SQL 有沒有寫錯都測得到。

單元測試在後端的價值相對低：service 層大多是薄薄一層轉發到 Prisma，把 Prisma mock 掉之後，測的其實是 mock 本身。

**例外：** 當 service 裡有真正的商業邏輯（計算分數、判斷條件跳題），那時單元測試才划算。

---

## 踩到的坑

| 症狀 | 根因 | 解法 |
| --- | --- | --- |
| `dist/main.js` 不存在，變成 `dist/src/main.js` | 根目錄的 `prisma.config.ts` 把 tsc 推導的 `rootDir` 撐大 | `tsconfig.build.json` 的 `exclude` 加入 `prisma.config.ts` |
| `ReferenceError: exports is not defined` | Prisma 產生 ESM，NestJS 編譯 CJS | `schema.prisma` 加 `moduleFormat = "cjs"` |
| Jest `Cannot find module '../generated/prisma/client.js'` | Jest 不會把 `.js` 副檔名解析回 `.ts` | jest 設定加 `moduleNameMapper: {"^(\\.{1,2}/.*)\\.js$": "$1"}` |
| `A dynamic import callback was invoked without --experimental-vm-modules` | Prisma 7 的 WASM 查詢編譯器用動態 `import()`，Jest 的 CJS 沙箱不支援 | 測試指令改用 `node --experimental-vm-modules node_modules/jest/bin/jest.js` |

> 這四個坑全部來自「Prisma 7 太新，與 NestJS 的預設設定衝突」。它們是**一次性成本** —— 設定已寫死在 repo 裡，之後不會再遇到。

---

## 作業

1. `pnpm start:dev`，開 `api.http` 送出 `GET /health`，確認回 200。
2. 故意把 `.env` 的 `DATABASE_URL` 改錯一個字元，重跑 —— **觀察錯誤發生在哪一刻**：是啟動時就失敗，還是第一次請求才失敗？為什麼？
3. 把 `health.controller.ts` 建構子的型別註記 `: PrismaService` 拿掉，看 Nest 報什麼錯 —— 這會讓你親眼看到上面提到的 metadata 機制。

### 解答

**第 1 題** — `{ "status": "ok", "database": "connected", ... }`。

**第 2 題 — 錯誤發生在「第一次請求」，不是啟動時。**

```text
ERROR [ExceptionsHandler] PrismaClientKnownRequestError:
Raw query failed. Code: `28P01`. Message: `password authentication failed for user 'neondb_owner'`
```

`28P01` 是 Postgres 的 `invalid_password`，代表 TCP 連線**有到 Neon**、握手也成功，是驗證那一步才被拒絕 —— 不是「找不到主機」。

原因就是上面 Driver Adapter 那節說的 lazy 連線：`pg.Pool` 建立時不開任何實體連線，`onModuleInit` 的 `$connect()` 只是把 adapter 準備好。真正拿憑證去敲門的時刻，是 `/health` 裡那句 `$queryRaw`。

**這題真正要學的不是答案，是它的維運意涵：**

- 密碼錯的服務會**正常啟動**。如果容器健康檢查只看「process 活著」或「port 開著」，它會被標成 healthy，然後每個真實請求都 500。
- 這正是為什麼 `/health` 要打一次真查詢，而不是直接 `return { status: 'ok' }`。第 1 題看到的 ok 是有含金量的 ok。
- 對照組：`PrismaService` 的 `ConfigService.getOrThrow('DATABASE_URL')` 是 fail-fast，變數**沒設**會在啟動時就死。但變數**設錯**只能靠實際連線才知道。**「存在」與「正確」是兩種驗證，只有前者能在啟動時免費做掉。**

**第 3 題 — Nest 找不到注入 token。**

```text
ERROR [ExceptionHandler] UnknownDependenciesException [Error]:
Nest can't resolve dependencies of the HealthController (?).
Please make sure that the argument at index [0] is available in the current module.
```

Nest 的注入 token 來自 `emitDecoratorMetadata` 在編譯期寫入的 `design:paramtypes`，它記錄建構子每個參數的型別，而型別的唯一來源就是那個 `: PrismaService`。拿掉之後 metadata 那格是空的，Nest 只好回報「index [0] 是 undefined」。

順著這條線可以推出三個之後一定會遇到的坑：

| 寫法 | 為什麼壞 |
| --- | --- |
| `import type { PrismaService }` | `import type` 編譯後整行消失，執行期沒有這個值可當 token —— 症狀與本題一模一樣 |
| 注入 interface | interface 編譯後不存在，metadata 只會是 `Object`；必須改用 `@Inject('TOKEN')` 手動指定 |
| 注入泛型（如 `Repository<User>`） | 型別擦除後只剩 `Repository`，區分不出來 |

> 這次只在執行期爆炸、沒被 tsc 擋下，是因為 `tsconfig.json` 設了 `noImplicitAny: false`（NestJS CLI 的預設）。開啟後這個錯會提早到編譯期。

