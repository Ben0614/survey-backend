# 複製這個檔案為 .env 並填入實際值。
# 這個範本會進版控，.env 不會 —— 所以這裡只放 key 的形狀，不放真實密碼。

PORT=3100
# 本機用 3100（3000 被 Nuxt 佔用）。
# **正式環境不要設這個變數** —— Render 之類的平台會自己注入 PORT，
# main.ts 用 `process.env.PORT ?? 3000` 接。自己設反而會蓋掉平台給的值。

# Neon Postgres 連線字串，從 Neon Console 的 Connection Details 複製。
# 本機開發指向 dev branch（見下面的分工表）。
DATABASE_URL="postgresql://<user>:<password>@<host>/<database>?sslmode=verify-full&channel_binding=require"

# ------------------------------------------------------------
# 三條 Neon branch 的分工（Ch8 定下來的）
# ------------------------------------------------------------
#   branch       誰在用                          會不會被清空
#   production   線上服務（填在 Render 的環境變數）  不會
#   dev          本機開發（這個 .env）              不會，但可以隨便玩
#   test         E2E 測試（.env.test）             每個測試案例前 TRUNCATE 四張表
#
# 三條的 host 前綴互不相同，這就是防呆 —— 貼上去之後回頭比對一次 host。
#
# 最危險的填錯法是「正式環境填成 test branch」：網站一切正常，
# 直到你在本機跑一次 pnpm test:e2e，線上資料在 70 秒內被清空，
# 而測試會顯示全部 passed。test/setup-env.ts 的防呆只看得到本機這兩個檔案，
# **看不到部署平台上填了什麼**。
#
# 另外注意：Neon 的**預設 branch 就叫 production**，但它不見得是你的正式環境 ——
# 這個專案原本拿它當開發庫用了七章。名字不代表用途，要看 host。
# ------------------------------------------------------------

# 設成 1 才會把 Prisma 實際送出的 SQL 印到終端機（Ch3 第二段加的）。
# 沒設或設成別的值就是關閉 —— 平常開著會把測試輸出淹掉。
# 它對 pnpm test:e2e 也有效：測試裡的 AppModule 同樣用 ConfigModule 讀 .env，
# 只有 DATABASE_URL 被 setup-env.ts 先灌進去的 .env.test 搶住（dotenv 不覆寫已存在的變數）。
PRISMA_LOG_QUERIES=

# 連線池上限（Ch5 加的）。沒設就用 pg 的預設值 10。
#
# **正式環境維持不設** —— Ch8 實測之後才這樣寫的：
# 連線字串帶 -pooler 時，中間隔著一層 PgBouncer。12 條並發請求進來時，
# PgBouncer 對 Postgres 只開了 7 條，而那台 compute 的 max_connections 是 901。
# 兩個數字管的是不同的兩段：
#     DATABASE_POOL_MAX  →  你的應用 到 PgBouncer
#     max_connections    →  PgBouncer 到 Postgres
# 免費方案只有一個實例，離上限遠得看不到。**沒有觀測到瓶頸就不要調數字。**
#
# 只有 .env.test 需要設成 1 —— 見下面那一段。
DATABASE_POOL_MAX=

# ------------------------------------------------------------
# .env.test —— E2E 測試專用，同樣不進版控，換機器時要自己重建
# ------------------------------------------------------------
# 只需要 DATABASE_URL 一個變數，但**必須指向 test branch**：
# E2E 測試每個案例前都會 TRUNCATE 四張表，指到別條就會把那條的資料清光。
#
# 建立方式：Neon Console → Branches → New branch → 命名 test，
# 複製該 branch 的連線字串（host 會跟上面那條不一樣）。
#
# test/setup-env.ts 會檢查這兩件事，不符合就讓測試直接失敗：
#   1. .env.test 存在
#   2. 它的 DATABASE_URL 與 .env 的不相同
#
# 另外建議加一行 DATABASE_POOL_MAX=1：
# e2e 是單執行緒跑的（jest-e2e.json 的 maxWorkers: 1），一次只有一個請求在飛，
# 開 10 條連線有 9 條從頭到尾閒著。詳見 docs/專案速查.md 的
# 「e2e 測試連線問題怎麼查」。
#
#   DATABASE_URL="postgresql://<user>:<password>@<test-host>/<database>?sslmode=require"
#   DATABASE_POOL_MAX=1
